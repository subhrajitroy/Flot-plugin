This plugin helps user specify mutiple constraints/thresholds on the plotting data along with colr
to be each constraint. 
The plugin will then ensure that portions of the graph that satisfy different constraints/ thresholds 
will be plotted with different colors as specified by the user.
